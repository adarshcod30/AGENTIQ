# Everything the image does not need. Keeps the build context small and, more
# importantly, guarantees a local .env can never be baked into a layer.
.env
*.env
!.env.example
.git
.github
node_modules
**/node_modules
web
fixtures
evaluation
docs
coverage
**/coverage
dist
**/dist
*.md
.claude
